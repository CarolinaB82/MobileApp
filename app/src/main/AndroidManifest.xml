<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"

        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        tools:targetApi="31">

        <activity
            android:name=".AccueilActivity"
            android:exported="true">


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CarteActivity"
            android:label="HellPlan"/>
        <activity android:name=".ProgActivity"
            android:label="HellProg"/>
        <activity android:name=".PopCornActivity"
            android:label="@string/PopCorn_titre"/>
        <activity android:name=".AlerteActivity"
            android:label="HellAlerte"/>
        <activity android:name=".AnnuaireActivity"
            android:label="HellAnnuaire"
        android:theme="@style/annuaire_orange"/>
        <activity android:name=".FestivalierActivity"
            android:label="HellFestivalier"/>



       <!--     android:name=".MainStageActivity"
            android:exported="true">
    <intent-filter>
        <action android:name="android.intent.action.VIEW" />-->
        <!--type mime c'est comme quand on ouvre un fichier dans le navigateur-->
       <!-- <data android:mimeType="image/jpeg" android:scheme="content"/>
    </intent-filter>
        </activity>-->

    </application>

</manifest>